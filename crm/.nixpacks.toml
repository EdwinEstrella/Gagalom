[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x", "openssl"]

[phases.build]
cmds = [
  "npm ci",
  "npx prisma generate",
  "npm run build"
]

[phases.start]
cmds = ["npm run start"]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
PORT = "3000"
